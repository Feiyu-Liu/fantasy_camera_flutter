# Auth

Authentication feature boundary.

Recommended layout when implementation starts:

- `data/`: auth repositories, API/local data sources, DTOs
- `domain/`: auth entities and use cases
- `presentation/`: login, account, and session UI/view models
