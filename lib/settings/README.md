# Settings

Settings feature boundary.

Recommended layout when implementation starts:

- `data/`: settings persistence and repositories
- `domain/`: settings models and use cases
- `presentation/`: settings screens, sections, and view models
